# M1.15 Validation Matrix

The package contains 84 blocking positive controls, seven fail-closed negative controls, and 20 detailed evidence result sets. Validation covers contract registry state, latest/archive/comparison cardinality and grains, archive immutability, field and payload lineage, matched baseline/stress identities, deterministic row and set hashes, downstream boundaries, and blocking configuration errors.
