# M1.13 - Exposure, Recovery & Expected Loss Foundations - Public Correction History

PostgreSQL does not support max(boolean); the pre-commit failure was recovered and the accepted source uses bool_or(boolean).

This public summary preserves the engineering lesson without duplicating every private audit artifact. The accepted package revision is `v0.2R1`.
