# M1.12 Accepted Release Notes — v0.2R1

M1.12 is **PASSED AND ACCEPTED**.

Accepted scope includes 1,500 scenario-aware integrated-risk snapshots, 10,500 component records, seven transparent components, explicit evidence gating, matched stress floors, deterministic hashes, 80 positive controls, and seven negative controls.

The only R1 correction is the read-only detail report's final query: `error_id` was replaced with `resolution_error_id`. No persistent data or business logic changed.

Next authorized module: M1.13 — Exposure, Recovery & Expected Loss Foundations.
