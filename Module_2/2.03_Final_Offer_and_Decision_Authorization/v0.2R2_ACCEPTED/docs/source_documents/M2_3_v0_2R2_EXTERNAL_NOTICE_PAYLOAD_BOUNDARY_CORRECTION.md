# M2.3 v0.2R2 External-Notice Payload Boundary Correction

The v0.2R1 payload assertion rejected `external_notice`, while the governed negative control and physical schema-boundary vocabulary use `external_notice_payload`. v0.2R2 adds that exact key and aligns `account_number`. No decision row, policy value, hash, positive evidence, or lifecycle state changes.
